# todo-list

## Run the project

- clone the repository
- run `npm install` to install dependencies
- run `npm run client` to start the front-end application on port 3000
- run `npm run server` to start the api on port 3001
